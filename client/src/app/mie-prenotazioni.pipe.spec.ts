import { MiePrenotazioniPipe } from './mie-prenotazioni.pipe';

describe('MiePrenotazioniPipe', () => {
  it('create an instance', () => {
    const pipe = new MiePrenotazioniPipe();
    expect(pipe).toBeTruthy();
  });
});
