import { VisNotificationsPipe } from './vis-notifications.pipe';

describe('VisNotificationsPipe', () => {
  it('create an instance', () => {
    const pipe = new VisNotificationsPipe();
    expect(pipe).toBeTruthy();
  });
});
